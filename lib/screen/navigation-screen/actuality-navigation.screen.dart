import 'package:flutter/material.dart';

class ActualityNavigationScreen extends StatelessWidget {
  const ActualityNavigationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text("Actuality")),
    );
  }
}
